# Copy to .env for local development; never commit the populated file.
MARKET_DATA_TOKEN=
# Research API cache lifetime in seconds; set 0 to disable, max 300.
RESEARCH_CACHE_TTL_SECONDS=30
# TUSHARE_TOKEN is accepted as an alternative name.
# TUSHARE_TOKEN=
