global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: quant-agent-api
    metrics_path: /internal/metrics
    static_configs:
      - targets: ["api:8000"]
