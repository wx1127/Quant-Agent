# Changelog

## Unreleased — 真实数据、市场状态与回测基础

- 新增带租约、检查点、幂等键、分页审计和失败重放的 RAW-first 增量同步；
- 新增 Tushare 股票/ETF/指数主数据与日线、交易日历和股票复权因子同步；
- 新增整理记录到原始响应的可追溯血缘及 0002～0004 数据库迁移；
- 新增 `quant-agent data sync`、`data status` 和 Point-in-time `research demo`；
- 新增滚动特征框架、市场趋势、市场宽度、行业强度和五类市场状态；
- 新增市场状态确认、防抖、严重下跌快速路径和可验证事件哈希链；
- 新增不可变回测事件、交易规则接口、版本化费用规则和确定性回放契约。
- 新增主线、龙头、候选评分以及严格 PIT 的 5/10/20 日历史评测；
- 新增公告证据、外部文本安全、个股强度、可交易性和基本面质量链路；
- 新增向量化与事件驱动回测、中国市场规则、ETF 轮动和主线龙头目标策略；
- 新增滚动前推、参数注册冻结、样本外结果和参数扰动稳定性分析；
- 新增统一回测验收报告，覆盖成本前后收益、风险、市场阶段、不可成交率和失败时期，
  并以同一规范日序列保证图表与表格一致；
- 新增不可变账户/持仓快照，分离可用、冻结和未交收数量并绑定估值来源与内容哈希；
- 新增 ETF 核心与股票增强的隔离预算聚合、PIT 行业约束、组合差异和含现金腿换手；
- 新增独立、失败关闭的风控契约与组合风控，覆盖总仓位、ETF/股票、行业、换手、
  市场状态预算和回撤停止新增风险，并允许受控减仓修复已有超限。
- 新增不可执行订单草案，将通过风控的组合差异转换为整手买卖估算，绑定 PIT 行情、
  交易/费用/滑点规则和全链路哈希，并按保护价校验可用资金与账户可卖数量。
- 新增无券商权限的模拟账户与模拟撮合，覆盖部分/无法成交、费用、现金和 lot 持仓守恒、
  T+1、行情时效、订单到期与跨快照多批次推进；提交前按成交重放唯一账户状态，并通过
  SQLite 事务持久化账户、回执及幂等/批次索引，保证进程重启后不会重复成交。
- 新增独立订单、逐笔成交、费用、现金和持仓核对，保留重复/冲突回报及差异证据，
  严重差异生成绑定完整结果的停止新增订单信号；精确小数计算不依赖全局 Decimal 精度。
- 新增全局/账户 Kill Switch 状态、事故证据、恢复审批和事件哈希链；Paper 下单入口
  强制检查闸门决策，并在下单提交期间保持闸门锁。恢复默认拒绝，必须由可信认证适配器
  验证人工风控管理员及审批，并在仓储锁内检查审批有效期和当前状态版本。
- 新增 SQLite Kill Switch 持久化、跨连接下单互斥、严格存储绑定和损坏检测，
  覆盖重启后阻断重放、动态检查时钟下的合法重试及恢复后排队事故重新激活。
- 新增 P6 不可变决策快照，绑定决策 ID、模式与时点、已验证数据版本及内容哈希、策略配置与
  登记参数、风险政策、账户快照、完整代码提交、代码制品以及 Agent/模型版本；严格 JSON
  往返和内容哈希拒绝字段缺失、类型混淆及身份篡改。
- 新增决策快照创建/恢复服务和内存/SQLite write-once 仓储；创建时复核权威数据清单完整性及
  账户、策略、参数和风险来源，恢复时拒绝输入漂移，同 ID 异内容拒绝覆盖，并支持重启恢复、
  跨连接原子创建、数据库级更新/删除/替换阻断和损坏存储失败关闭。
- 新增 P6 Agent 工具注册与中央权限策略，固定 17 个 v1 工具名称及五种运行模式矩阵，构建时
  绑定可信 principal、capability 和账户范围；`LIVE_AUTO` 保持空白名单，人工审批、模式切换、
  风控修改和 Kill Switch 恢复等管理入口禁止注册。
- 新增严格工具参数/输出契约、载荷资源上限、决策快照恢复绑定、幂等键检查、同步允许/拒绝审计
  和实盘一次性授权租约；`submit_paper_orders` 仅属于 `PAPER`，
  `submit_approved_orders` 仅属于 `LIVE_ASSISTED`，后者的租约覆盖允许审计和同步 handler 执行。
  当前不包含真实券商执行 handler 或生产级审批基础设施；JSONL 审计仅支持单进程线程安全整行追加，
  不将其描述为生产级跨进程、持久化或不可篡改审计。
- 新增 P6 七个决策绑定的只读研究工具，覆盖市场快照、数据质量、市场阶段、市场主线、主题龙头、
  股票候选和候选解释；通过可信 `ResearchInputSource` 驱动既有确定性引擎，完整重放 PIT 市场阶段
  与主线历史，不复制评分公式，也不把未校准候选分数包装成概率。
- 新增研究工具的严格输出投影、标准失败语义、数据版本/内容哈希验证和账户快照作用域；数据不可用、
  输入错位、质量门禁失败与候选不存在均显式返回错误。生产 Parquet/Arrow 领域解码 source 尚未
  交付，因此不会自动注册合成或占位 handler。
- 新增 P6 七个决策绑定的组合、风控、订单草案、模拟提交与账户核对工具；严格验证账户、决策、
  策略、风控政策、数据版本及前序制品哈希，风险 `REJECT/ERROR` 不得创建订单草案。
- 强化订单草案与模拟回执血缘：草案按冻结决策时点评估并绑定同一风控结果哈希，模拟成交前后
  账户状态显式携带账户、模式、数据版本、来源快照、币种与时点，运行时逐层核验后才允许核对。
- 新增账户/决策作用域的草案工件仓储与受控 paper gateway；写操作强制幂等键，并通过首次写入
  语义处理并发重试。模拟提交必须经过 `PaperExecutionService`，保留 Kill Switch 锁、CAS、批次
  唯一消费和回执恢复；`PAPER`/`LIVE_ASSISTED` 权限隔离，核对只返回停止信号而不隐式改变开关。
- 新增 P6 显式 Agent Runtime，为研究、回测、组合报告、订单草案、PAPER 执行和
  LIVE_ASSISTED 执行定义目标相关状态路径；模型可见目录与当前阶段、前置制品及中央工具权限取
  交集，风控、审批、提交和核对阶段不能跳过。
- 新增不可变调用预算、非法调用预算、独立核对预算、总超时、审批超时及协作式取消；执行前进入
  明确终态，订单可能已经提交后则必须核对，结果未知、严重差异或核对预算耗尽统一进入
  `INCIDENT`。外部批准严格绑定决策、草案批次、控制证据哈希和有效期。
- 新增内容寻址、前序哈希连接的 Agent 运行事件、内存 CAS 仓储和纯函数轨迹回放；回放不调用工具
  或重复副作用，并重新验证身份、状态边、调用配对、制品、审批、时间和预算约束。生产级跨进程
  轨迹持久化、原始载荷留存/脱敏及聚合审计仍由后续 P6-T09 交付。
- 新增 P6 Agent 输出契约，将模型草稿与可信发布分层，强制分离事实、推断、反对证据、风险和
  失效条件；决策身份、运行状态与数据截止时间只能由已验证快照注入。
- 新增工具响应级证据引用，绑定请求、工具、响应哈希、RFC 6901 数据路径、路径值哈希和数据版本；
  数字只能通过精确匹配来源标量的结构化指标发布。缺少或漂移证据固定降级为“证据不足”与
  `NO_ACTION`，工具警告/错误不会被静默丢弃，并拒绝保证性措辞及 Unicode/空白混淆变体。

## 0.3.0 — P1.5 可运行化

- 新增 `quant-agent` / `python -m quant_agent` 命令入口；
- 新增确定性的 Provider 到 Parquet/DuckDB 演示流水线；
- 接通受控环境变量覆盖、`env://` 与可注入的 `vault://` 解析；
- 本地默认改为零配置 SQLite，并修复 Alembic 目录创建与 URL 覆盖；
- 增加 PostgreSQL Psycopg 驱动、Windows 时区依赖与可选 Compose 服务；
- 增强快照路径校验、数据库登记恢复和冲突检测。

## 0.2.0 — P1 数据底座

- 新增 SQLAlchemy 数据模型和 Alembic 初始迁移；
- 新增交易日历、稳定证券 ID、历史代码和证券状态区间；
- 新增统一 Provider 契约、Fake Provider 和 Tushare 日线适配器；
- 新增原始响应归档和幂等日线入库；
- 新增独立复权因子、公司行动及前/后复权研究视图；
- 新增 Point-in-time 财务修订查询；
- 新增历史行业分类与成员关系；
- 新增数据质量引擎；
- 新增 Parquet 不可变快照、内容校验和 DuckDB 查询。

## 0.1.0 — P0 工程基础

- 初始化项目结构和 Miniconda 开发环境；
- 建立依赖、格式、静态检查、类型检查、测试和 CI；
- 建立公共领域契约；
- 建立环境配置、密钥引用、日志、追踪、脱敏和审计基础。

